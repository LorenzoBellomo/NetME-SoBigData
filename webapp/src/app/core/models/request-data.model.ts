export interface RequestData {
    searchOn: string;
    searchType: string;
    papersNumber: string;
    sortType: string;
    queryMode: string;
    input: string | File;
    networkName: string;
}